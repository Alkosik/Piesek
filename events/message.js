module.exports = {
	name: 'messageCreate',
	execute(message) {
		// console.log(message);
		// console.log(`${message.author.tag} in #${message.channel.name} sent: ${message.content}`);
        // This functionality will be removed after aprioprate tests are conducted.
		//console.log("rucham kurwy")
	},
};